{
  "version": 2,
  "builds": [
    {
      "src": "frontend_reconstruction/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist",
        "cwd": "frontend_reconstruction"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "env": {
    "VITE_API_BASE_URL": "https://translatoragent-production.up.railway.app",
    "VITE_ENABLE_API_INTEGRATION": "true"
  }
}